# Optional high-quality local AI stack.
# Install this in a separate runtime from the API if possible.
#
# Recommended CUDA torch install for RTX 2060 / NVIDIA driver CUDA 13.x:
# pip install torch==2.8.0+cu128 torchaudio==2.8.0+cu128 --extra-index-url https://download.pytorch.org/whl/cu128
# pip install torchcodec==0.7.0
#
# Then install:
f5-tts==1.1.20
mediapipe>=0.10.14
transformers>=4.51.0
accelerate>=1.6.0
safetensors>=0.5.0
